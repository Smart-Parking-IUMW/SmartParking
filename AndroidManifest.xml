<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartparking">
    <!--
        The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
        Google Maps Android API v2, but you must specify either coarse or fine
        location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckOut"></activity>
        <activity android:name=".Search" />
        <activity android:name=".HomePage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".ReservationDetails" />
        <activity android:name=".Reservation" />
        <activity android:name=".RateComments" />
        <activity android:name=".CheckPayment" />
        <activity android:name=".ProfileGender" />
        <activity android:name=".ProfilePassword" />
        <activity android:name=".ProfileEmail" />
        <activity android:name=".ProfileUsername" />
        <activity android:name=".ProfilePhoto" />
        <activity android:name=".SettingsFAQ" />
        <activity android:name=".SettingsReport" />
        <activity android:name=".SettingsCountry" />
        <activity android:name=".SettingsVersion" />
        <activity android:name=".SettingsLanguage" />
        <activity android:name=".Scan" />
        <activity android:name=".MyWalletRequest" />
        <activity android:name=".MyWalletTopUp" />
        <activity android:name=".MyWalletTransfer" />
        <activity android:name=".MyWalletPay" />
        <activity android:name=".About" />
        <activity android:name=".ContactUs" />
        <activity android:name=".News" />
        <activity android:name=".Promotion" />
        <activity android:name=".History" />
        <activity android:name=".MyWallet" />
        <activity android:name=".Settings" />
        <activity android:name=".Notification" />
        <activity android:name=".Profile" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MyWalletTransferAmount" />
        <activity android:name=".SignUp" />
        <activity android:name=".LogIn" />
        <activity android:name=".FirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>